# Vite dev server
VITE_API_BASE_URL=/api
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173

# Comma-separated hostnames allowed by Vite's host check
# e.g. myhost.duckdns.org,localhost
VITE_ALLOWED_HOSTS=localhost
